apiVersion: v1
kind: Service
metadata:
  name: kk-payments
  namespace: kijani-staging
  labels:
    app: kk-payments
    environment: staging
spec:
  type: ClusterIP

  selector:
    app: kk-payments

  ports:
    - name: http
      port: 3001
      targetPort: 5000
      protocol: TCP
